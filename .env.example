# Supabase 配置
# 复制此文件为 .env 并填入实际值

# Supabase 项目 URL
VITE_SUPABASE_URL=https://your-project-id.supabase.co

# Supabase Anon Key（客户端使用）
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Supabase Service Role Key（仅服务端使用，不要在客户端代码中！）
VITE_SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key_here

# 官方智谱 API Key（游客模式使用）
# ⚠️ 此 Key 仅用于游客模式，请勿泄露
ZHIPU_OFFICIAL_API_KEY=your_zhipu_api_key_here

# 阿里云 OSS 配置（用于上传安装包）
# 获取方式：访问 https://ram.console.aliyun.com/ → 创建 RAM 用户 → 获取 AccessKey
ALI_OSS_ACCESS_KEY_ID=your_access_key_id_here
ALI_OSS_ACCESS_KEY_SECRET=your_access_key_secret_here

# ⚠️ 安全警告：
# 1. .env 文件已添加到 .gitignore
# 2. 永远不要提交 .env 文件到 Git
# 3. Service Role Key 仅在 Electron 主进程或 Edge Functions 中使用
# 4. 前端代码只能使用 Anon Key
# 5. 阿里云 AccessKey 仅用于上传安装包，不要在客户端代码中使用
# 6. 官方 API Key 存储在环境变量中，不要硬编码在代码里
