# 快速入门

> **适用版本**: v2.6.3+
> **阅读时间**: 5分钟
> **前置知识**: 无

---

## 项目简介

**小白AI** 是一款零代码 AI 助手桌面应用，帮助不懂代码的用户也能轻松使用 Claude AI。

### 核心特性
- 🤖 **系统级 AI 助手** - 帮你操作电脑、管理文件
- 💬 **自然对话** - 像聊天一样使用 AI
- ☁️ **云端同步** - 对话历史自动保存，跨设备访问
- 🔐 **游客模式** - 无需注册即可免费使用 10 次
- 📱 **手机登录** - 手机号 + 验证码，无需密码

### 产品定位
> **降低AI使用门槛，让每个人都能轻松用上AI**

---

## 技术栈

| 类别 | 技术 | 用途 |
|-----|------|------|
| **桌面框架** | Electron | 跨平台桌面应用 |
| **前端** | React + Vite | UI 渲染 |
| **样式** | CSS | 界面样式（绿色主题） |
| **后端** | Supabase | 云端数据库、认证 |
| **AI** | Claude Agent SDK | AI 对话能力 |
| **本地存储** | SQLite (better-sqlite3) | 本地缓存 |
| **短信服务** | 阿里云 SMS | 验证码发送 |

---

## 开发环境搭建

### 前置要求

```bash
# 1. 安装 Node.js (推荐 v18+)
node --version

# 2. 安装 npm (Node.js 自带)
npm --version

# 3. 推荐：安装 nvm（Node 版本管理器）
# macOS/Linux
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash

# Windows
# 下载安装器：https://github.com/coreybutler/nvm-windows
```

### 克隆项目

```bash
# 1. 进入项目目录
cd /Users/shawn/Downloads/小白AI

# 2. 安装依赖
npm install

# 3. 启动开发服务器
npm run dev
```

### 项目结构

```
小白AI/
├── docs/                    # 技术文档
│   ├── README.md           # 文档导航
│   ├── 01-快速入门.md
│   ├── 02-登录系统.md
│   └── ...
├── electron/               # Electron 主进程
│   ├── main.js            # 主进程入口
│   ├── preload.js         # 预加载脚本
│   └── ...
├── src/                    # 前端代码
│   ├── components/        # React 组件
│   │   ├── ChatArea.jsx
│   │   ├── InputArea.jsx
│   │   ├── LoginModal.jsx
│   │   └── ...
│   ├── lib/               # 工具库
│   │   ├── supabaseClient.js    # Supabase 客户端
│   │   └── cloudService.js      # 云端服务
│   ├── contexts/          # React Context
│   │   └── AuthContext.jsx      # 认证状态管理
│   ├── App.jsx           # 主应用
│   └── main.jsx          # 入口文件
├── supabase/             # Supabase 配置
│   └── migrations/       # 数据库迁移文件
├── public/               # 静态资源
│   └── logo.svg
├── package.json
└── electron-builder.yml  # 打包配置
```

---

## 核心概念

### 1. Electron 架构

```
┌─────────────────────────────────┐
│   Electron 主进程 (Main)        │
│   - 窗口管理                     │
│   - 文件系统操作                 │
│   - IPC 通信                     │
└──────────┬──────────────────────┘
           │ IPC
┌──────────▼──────────────────────┐
│   渲染进程 (Renderer)           │
│   - React 应用                   │
│   - 用户界面                     │
│   - 与主进程通信                 │
└─────────────────────────────────┘
```

### 2. 认证系统

小白AI有两种使用模式：

| 模式 | 说明 | 数据存储 |
|-----|------|---------|
| **游客模式** | 无需登录，免费使用 10 次 | 云端（device_id） |
| **登录模式** | 手机号 + 验证码登录 | 云端（user_id） |

**重要**: 小白AI **没有密码**，只有验证码登录！

### 3. 数据架构

```
本地数据（SQLite）
├── 配置信息
├── API Keys
└── 本地缓存

云端数据（Supabase PostgreSQL）
├── user_profiles       # 用户资料（仅手机号）
├── conversations       # 对话历史
├── messages           # 消息记录
├── verification_codes # 验证码
└── guest_usage        # 游客使用统计
```

---

## 开发命令

```bash
# 开发模式（热重载）
npm run dev

# 构建
npm run build

# 打包（生成安装包）
npm run dist:mac       # macOS
npm run dist:win       # Windows

# 清理缓存
rm -rf node_modules/.vite dist
```

---

## 配置说明

### 必需配置

#### 1. Supabase 配置

文件：`src/lib/supabaseClient.js`

```javascript
const supabaseUrl = 'https://your-project.supabase.co';
const supabaseAnonKey = 'sb_publishable_YOUR_ANON_KEY_HERE';
const supabaseServiceRoleKey = 'sbp_YOUR_SERVICE_ROLE_KEY_HERE';
```

#### 2. 阿里云短信配置

环境变量（Supabase Dashboard → Settings → Secrets）：

```bash
ALIYUN_ACCESS_KEY_ID=你的AccessKeyID
ALIYUN_ACCESS_KEY_SECRET=你的AccessKeySecret
ALIYUN_SMS_SIGN_NAME=原则科技
ALIYUN_SMS_TEMPLATE_CODE=SMS_223880024
```

### 可选配置

#### 官方 API Key

文件：`electron/official-config.js`

```javascript
module.exports = {
  // 游客模式使用的官方 API Key
  zhipu: {
    apiKey: '你的智谱API Key',
    model: 'glm-4.7'
  }
};
```

---

## 常见问题

### Q: 应用启动失败？

```bash
# 1. 检查 Node 版本
node --version  # 推荐 v18+

# 2. 清理缓存并重新安装
rm -rf node_modules package-lock.json
npm install

# 3. 检查端口占用
lsof -i :5173
```

### Q: 登录失败？

1. 检查 Supabase 配置是否正确
2. 查看 [登录系统文档](./02-登录系统.md)
3. 检查数据库是否有验证码记录

### Q: 数据库连接失败？

1. 确认 Supabase 项目状态
2. 检查网络连接
3. 查看浏览器控制台错误信息

### Q: 如何调试？

```bash
# 1. 开发模式下自动打开 DevTools
npm run dev

# 2. 查看主进程日志
# 在终端中查看 Console 输出

# 3. 查看渲染进程日志
# 在浏览器 DevTools 中查看 Console
```

---

## 下一步

### 新手推荐路径

1. **了解架构** → 阅读 [系统架构](./06-系统架构.md)
2. **理解登录** → 阅读 [登录系统](./02-登录系统.md)
3. **学习数据库** → 阅读 [数据库设计](./03-数据库设计.md)
4. **开始开发** → 阅读 [开发规范](./08-开发规范.md)

### 按需学习

- 想修改登录逻辑 → [登录系统](./02-登录系统.md)
- 想添加数据库字段 → [数据库设计](./03-数据库设计.md)
- 想实现新功能 → [开发规范](./08-开发规范.md)
- 想部署到生产 → [部署与配置](./07-部署与配置.md)

---

## 有用的链接

- **项目仓库**: https://github.com/Shanw26/xiaobaiAI
- **Supabase 文档**: https://supabase.com/docs
- **Electron 文档**: https://www.electronjs.org/docs
- **Claude 文档**: https://docs.anthropic.com

---

**最后更新**: 2026-01-07
**维护者**: Claude Code + 晓力
