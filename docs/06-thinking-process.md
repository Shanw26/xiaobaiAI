# AI 思考过程展示功能

> **适用版本**: v2.7.5+
> **最后更新**: 2026-01-07
> **功能类型**: AI交互优化

---

## 📋 功能概述

AI在回答技术问题时，会先展示简洁的思考过程，让用户了解AI的分析逻辑，增加用户信任感。

### 核心价值

- ✅ **透明度**：用户可以看到AI的思考过程
- ✅ **信任感**：了解AI的分析逻辑，更放心
- ✅ **效率**：快速判断AI是否理解问题
- ✅ **学习性**：用户可以学习AI的思考方式

---

## 🎯 使用场景

### 何时展示思考过程

| 场景 | 是否展示 | 原因 |
|-----|---------|------|
| 技术问题 | ✅ 是 | 需要展示分析逻辑 |
| 代码修改 | ✅ 是 | 涉及技术决策 |
| 文件操作 | ✅ 是 | 涉及系统操作 |
| 系统配置 | ✅ 是 | 涉及安全风险 |
| 简单问候 | ❌ 否 | 无需展示 |
| 纯闲聊 | ❌ 否 | 自然对话 |

### 示例问题

**会展示思考过程**：
- "帮我创建一个Python脚本爬取网页数据"
- "如何修改这个组件的样式？"
- "把这段代码优化一下"
- "帮我配置Nginx反向代理"

**不会展示思考过程**：
- "你好"
- "今天天气怎么样"
- "谢谢"
- "再见"

---

## 📐 思考过程格式

### 标准格式

```markdown
**分析**：快速说明问题本质（1-2句话）

**方案**：直接说明解决方法（1-2句话）

**注意**：关键风险点或注意事项（1-2点，让用户放心）

**预期**：会得到什么结果（1句话）
```

### 实际示例

**用户问题**：帮我创建一个Python脚本爬取网页数据

**AI回答**：

```markdown
**分析**：用户需要一个网页爬虫脚本，用于获取网站数据

**方案**：创建Python脚本，使用requests库获取HTML，用BeautifulSoup解析数据

**注意**：
- 需要遵守目标网站的robots协议
- 添加请求头避免被反爬虫拦截
- 用户需要确认目标网站URL

**预期**：生成一个可直接运行的爬虫脚本，用户只需修改目标网址
```

好的，我来帮你创建这个爬虫脚本...

---

## 🔧 技术实现

### 1. Agent System Prompt

**文件**: `electron/agent.js`

**位置**: 第507-542行

```javascript
const systemPrompt = `你是小白AI，一个基于 Claude Agent SDK 的 AI 助手。

## 核心能力
1. **文件操作**：可以创建、读取、编辑、删除文件
2. **用户信息管理**：可以记住用户的个人信息，提供个性化服务
3. **对话记忆**：通过全局设置和记忆文件，记住用户偏好

## 思考过程展示（重要）
在回答用户问题前，**必须先展示简洁的思考过程**，让用户了解你的分析逻辑。

### 思考过程格式
\`\`\`思考
**分析**：快速说明问题本质（1-2句话）

**方案**：直接说明解决方法（1-2句话）

**注意**：关键风险点或注意事项（1-2点，让用户放心）

**预期**：会得到什么结果（1句话）
\`\`\`

### 何时展示思考过程
- ✅ 用户提出技术问题
- ✅ 用户需要代码修改
- ✅ 涉及文件操作
- ✅ 涉及系统配置
- ❌ 简单问候（如"你好"）
- ❌ 纯闲聊（如"今天天气怎么样"）

### 示例
用户：帮我创建一个Python脚本爬取网页数据

\`\`\`思考
**分析**：用户需要一个网页爬虫脚本

**方案**：创建Python脚本，使用requests获取HTML，用BeautifulSoup解析数据

**注意**：遵守网站robots协议，添加请求头避免被封，用户需确认目标网站

**预期**：生成可运行的爬虫脚本，用户可直接使用
\`\`\`

好的，我来帮你创建... [后面是具体回答]

...（其他prompt内容）
`;
```

**关键点**：
- 使用 "必须" 强调重要性
- 提供清晰的格式示例
- 明确何时展示、何时不展示
- 给出具体的示例对话

---

### 2. 前端渲染

**文件**: `src/components/MarkdownRenderer.jsx`

#### ThinkingProcess 组件

**位置**: 第19-43行

```javascript
// 思考过程组件（支持折叠）
function ThinkingProcess({ children }) {
  const [isExpanded, setIsExpanded] = useState(true);

  const toggle = () => {
    setIsExpanded(!isExpanded);
  };

  return (
    <div className="thinking-process">
      <div className="thinking-header" onClick={toggle}>
        <span className="thinking-icon">🤔</span>
        <span className="thinking-title">思考过程</span>
        <span style={{ marginLeft: 'auto', fontSize: '0.8em' }}>
          {isExpanded ? '▼' : '▶'}
        </span>
      </div>
      {isExpanded && (
        <div className="thinking-content">
          {children}
        </div>
      )}
    </div>
  );
}
```

**关键点**：
- 使用 `useState` 管理折叠状态
- 点击标题栏切换展开/折叠
- 默认展开（`useState(true)`）
- 显示折叠状态图标（▼/▶）

#### Code 组件修改

**位置**: 第152-177行

```javascript
code({ node, inline, className, children, ...props }) {
  const match = /language-(\w+)/.exec(className || '');
  const language = match ? match[1] : '';
  const codeContent = String(children).replace(/\n$/, '');

  // 特殊处理：思考过程
  if (!inline && language === '思考') {
    return <ThinkingProcess {...props}>{children}</ThinkingProcess>;
  }

  return !inline && language ? (
    <SyntaxHighlighter
      style={oneDark}
      language={language}
      PreTag="div"
      className="code-block"
      {...props}
    >
      {codeContent}
    </SyntaxHighlighter>
  ) : (
    <code className={`inline-code ${className || ''}`} {...props}>
      {children}
    </code>
  );
}
```

**关键点**：
- 检测 `language === '思考'`
- 使用 `ThinkingProcess` 组件渲染
- 不使用 `SyntaxHighlighter`（思考过程不需要代码高亮）

---

### 3. CSS 样式

**文件**: `src/components/MarkdownRenderer.css`

**位置**: 第170-228行

```css
/* 思考过程样式 */
.thinking-process {
  margin: 1em 0;
  border: 1px solid #e0e0e0;
  border-radius: 8px;
  background: #fafafa;
  overflow: hidden;
}

.thinking-header {
  display: flex;
  align-items: center;
  padding: 0.75em 1em;
  background: linear-gradient(135deg, #f0f0f0 0%, #e8e8e8 100%);
  border-bottom: 1px solid #e0e0e0;
  cursor: pointer;
  user-select: none;
  transition: background 0.2s ease;
}

.thinking-header:hover {
  background: linear-gradient(135deg, #e8e8e8 0%, #e0e0e0 100%);
}

.thinking-icon {
  font-size: 1.2em;
  margin-right: 0.5em;
}

.thinking-title {
  font-weight: 600;
  font-size: 0.95em;
  color: #666;
}

.thinking-content {
  padding: 1em;
  font-size: 0.95em;
  line-height: 1.6;
  color: #555;
  background: #fafafa;
}

.thinking-content strong {
  color: #333;
  font-weight: 600;
}

.thinking-content p {
  margin: 0.5em 0;
}

.thinking-content p:first-child {
  margin-top: 0;
}

.thinking-content p:last-child {
  margin-bottom: 0;
}
```

**关键点**：
- 灰色背景卡片（`#fafafa`）
- 渐变标题栏（从 `#f0f0f0` 到 `#e8e8e8`）
- 鼠标悬停时标题栏变深
- `user-select: none` 防止选中标题文字
- `cursor: pointer` 提示可点击

---

## 🎨 UI 效果

### 视觉展示

```
┌─────────────────────────────────────────────┐
│ 🤔 思考过程                              ▼  │
├─────────────────────────────────────────────┤
│ **分析**：用户需要一个网页爬虫脚本            │
│                                             │
│ **方案**：创建Python脚本，使用requests       │
│ 获取HTML，用BeautifulSoup解析数据            │
│                                             │
│ **注意**：                                   │
│ - 遵守网站robots协议                         │
│ - 添加请求头避免被封                         │
│                                             │
│ **预期**：生成可运行的爬虫脚本                │
└─────────────────────────────────────────────┘

好的，我来帮你创建这个爬虫脚本...
[后面是具体回答]
```

### 交互效果

1. **默认展开**：用户打开时思考过程是展开的
2. **点击折叠**：点击标题栏可以折叠/展开
3. **图标指示**：
   - ▼ = 展开状态
   - ▶ = 折叠状态
4. **悬停效果**：鼠标悬停在标题栏上时，背景变深

---

## 📊 数据流程

```
用户发送消息
    ↓
Agent 收到消息
    ↓
判断是否需要展示思考过程
    ├→ 技术问题/代码/文件操作 → ✅ 展示
    └→ 简单问候/闲聊 → ❌ 不展示
    ↓
AI 生成思考过程（markdown格式）
```思考
**分析**：XXX
**方案**：XXX
**注意**：XXX
**预期**：XXX
```
    ↓
AI 生成具体回答
    ↓
前端渲染 Markdown
    ↓
识别 ```思考 代码块
    ↓
使用 ThinkingProcess 组件渲染
    ↓
显示在界面上（可折叠）
```

---

## 🧪 测试验证

### 测试场景1：技术问题

**输入**：
```
帮我创建一个Python脚本读取CSV文件
```

**预期输出**：
```
┌─────────────────────────────────────┐
│ 🤔 思考过程                      ▼  │
├─────────────────────────────────────┤
│ **分析**：需要读取CSV文件并处理       │
│ **方案**：使用pandas库读取CSV       │
│ **注意**：需确认CSV文件格式和编码    │
│ **预期**：提供完整的读取和示例代码   │
└─────────────────────────────────────┘

好的，我来帮你创建这个脚本...
```

### 测试场景2：简单问候

**输入**：
```
你好
```

**预期输出**：
```
你好！我是小白AI，很高兴为你服务...
```
（不展示思考过程）

### 测试场景3：折叠/展开

**操作**：
1. 发送技术问题
2. 看到思考过程卡片（展开状态）
3. 点击标题栏
4. 思考过程折叠
5. 再次点击
6. 思考过程展开

---

## 🔍 常见问题

### Q1: 思考过程太长，影响阅读怎么办？

**A**: 用户可以点击标题栏折叠思考过程，只看具体回答。折叠后会记住状态（当前会话）。

### Q2: 所有问题都会展示思考过程吗？

**A**: 不会。只对技术问题、代码修改、文件操作、系统配置类问题展示。简单问候和闲聊不会展示。

### Q3: 思考过程的内容由谁控制？

**A**: 由 AI Agent 的 system prompt 控制。格式固定，但具体内容由 AI 根据问题生成。

### Q4: 可以自定义思考过程的样式吗？

**A**: 可以。修改 `src/components/MarkdownRenderer.css` 中的 `.thinking-process` 相关样式即可。

### Q5: 思考过程支持 markdown 格式吗？

**A**: 支持。思考过程内部可以使用加粗、列表等 markdown 格式，会被正常渲染。

---

## 🚀 未来优化方向

### 可能的改进

1. **个性化配置**：用户可以选择是否显示思考过程
2. **详细程度调整**：用户可以选择详细/简洁的思考过程
3. **多语言支持**：根据用户语言显示不同语言的思考过程
4. **思考历史**：保存思考过程，方便用户回顾
5. **思考链可视化**：对于复杂问题，展示多步推理过程

### 当前限制

- 思考过程格式固定，不够灵活
- 折叠状态不跨会话保存
- 无法自定义展示规则

---

## 📚 相关文档

- **[File Path Click](./05-file-path-click.md)** - Markdown 渲染相关
- **[System Architecture](./07-system-architecture.md)** - Agent 架构设计
- **[Development Guidelines](./09-development-guidelines.md)** - 开发规范

---

## 📝 更新日志

### v2.7.5 (2026-01-07)

**新增**：
- ✅ AI 思考过程展示功能
- ✅ 可折叠的思考过程卡片
- ✅ 思考过程样式优化

**修改**：
- 更新 Agent system prompt
- 扩展 MarkdownRenderer 组件
- 新增 CSS 样式

**影响**：
- AI 回答更透明，用户更放心
- 提升用户体验和信任度

---

**文档维护**: Claude Code + 晓力
**功能版本**: v2.7.5+
**最后更新**: 2026-01-07
