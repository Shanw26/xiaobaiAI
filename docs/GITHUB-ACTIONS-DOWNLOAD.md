# 如何下载 GitHub Actions 打包的安装包

## 📥 下载步骤

### 方法 1：通过 GitHub 网页下载（推荐）

#### 步骤 1：访问 Actions 页面
打开浏览器，访问：
```
https://github.com/Shanw26/xiaobaiAI/actions
```

#### 步骤 2：找到最新的 workflow run
你会看到类似这样的列表：
```
✓ Build Multi-Platform  ·  push  ·  v2.10.15
✗ Build Multi-Platform  ·  push  ·  v2.10.15
✓ Build Multi-Platform  ·  push  ·  v2.10.14
```

**点击最新的 v2.10.15 run**（即使显示失败 ❌，也要点击）

#### 步骤 3：查看 workflow jobs
进入后你会看到 3 个并行任务：
```
✓ build-windows    (完成)
✗ build-macos      (失败)
✗ build-linux      (失败)
```

**点击 `build-windows` 任务**

#### 步骤 4：下载 artifacts
滚动到页面底部，找到 "Artifacts" 区域：

```
Artifacts
─────────────────────────────────────
📦 windows-installer    228 MB  ·  expires in 30 days
```

**点击 `windows-installer` 下载**

### 方法 2：直接访问 URL（快捷）

如果是 v2.10.15，直接访问：
```
https://github.com/Shanw26/xiaobaiAI/actions/runs/最新ID
```

或者查看所有：
```
https://github.com/Shanw26/xiaobaiAI/actions/workflows/build.yml
```

### 方法 3：使用 GitHub CLI（如果已安装）

```bash
# 列出最近的 runs
gh run list --repo Shanw26/xiaobaiAI --limit 5

# 下载特定 run 的 artifacts
gh run download [RUN-ID] --repo Shanw26/xiaobaiAI
```

## 📦 下载后的文件

解压下载的 zip 文件后，你会得到：

```
windows-installer/
└── 小白AI Setup 2.10.13.exe  (约 228 MB)
```

## 🖥️ 在 Windows 上测试

1. **复制到 Windows 机器**
2. **双击安装**
3. **打开应用，发送一条消息**
4. **检查是否有 better-sqlite3 错误**

## ✅ 测试成功标志

如果能看到以下内容，说明成功：
- ✅ 应用正常启动
- ✅ 可以发送消息
- ✅ 没有 "better_sqlite3.node is not a valid Win32 application" 错误
- ✅ AI 可以正常回复

## 🆘 如果找不到 Artifacts

### 可能的原因：

1. **workflow 还在运行中**
   - 刷新页面等待完成
   - Windows 打包约需 10-15 分钟

2. **所有平台都失败**
   - 点击失败的 job 查看错误日志
   - 发给我诊断

3. **Artifacts 过期**
   - Artifacts 保留 30 天
   - 如果过期需要重新打包

4. **点击了错误的 run**
   - 确保点击的是 v2.10.15（不是 v2.10.14 或更早）
   - 查看 "Trigger" 列确认版本号

## 📸 截图说明

如果还是找不到，请：
1. 截图 Actions 页面
2. 发给我看
3. 我帮你定位

---

**当前版本**：v2.10.15
**下载链接**：https://github.com/Shanw26/xiaobaiAI/actions
